/**
 * Component registry — placeholder.
 *
 * 21st.dev and Aceternity UI are copy-paste component libraries, not
 * npm packages. When we want one of their components:
 *
 *   1. Copy the source file from the library's website.
 *   2. Drop it under components/ui/<component>.tsx.
 *   3. Ensure it imports `cn` from @/lib/utils.
 *   4. Export it from here if it's broadly reused.
 *
 * Existing components from earlier phases live alongside and don't
 * need to be re-exported through this registry unless they're used
 * as shadcn-style primitives.
 */

// Example future exports:
// export { Button } from "./Button";
// export { Spotlight } from "./Spotlight";
// export { Sparkles } from "./Sparkles";

export {};
